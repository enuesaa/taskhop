# run `gqlgenc`

model:
  filename: commander/gql/model/main.go
  package: model

client:
  package: client
  filename: runner/client/client.go

schema:
  - commander/gql/schema/*.graphql

query:
  - runner/documents/*.graphql

generate:
  clientInterfaceName: GraphQLClient
  structFieldsAlwaysPointers: true
