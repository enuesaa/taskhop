schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Query {
  health: Health!
}

type Mutation {
  runCmd(input: RunCmdInput!): Boolean!
}

type Subscription {
  subscribeRunCmdOutput: RunCmdOutput!
}
